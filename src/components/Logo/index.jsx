import './logo.css';

export default function Logo({ children }) {
    return (
        <div className="logo">
            {/* { children } */}

            <div>BGTech</div> <span>Experience</span>
        </div>
    );
}
